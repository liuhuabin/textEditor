# Txet-editor-with-accounts
A text editor which could save, open and edit files using GUI. User could register and login his own account. The password will be encrypted and then stored in the file "password.txt". Each user has his own folder to store his files.
